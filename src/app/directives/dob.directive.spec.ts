import { DobDirective } from './dob.directive';

describe('DobDirective', () => {
  it('should create an instance', () => {
    const directive = new DobDirective();
    expect(directive).toBeTruthy();
  });
});
